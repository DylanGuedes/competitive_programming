#include <bits/stdc++.h>
#ifdef JUDGE
std::ifstream cin("aplusb.in");
std::ofstream cout("aplusb.out");
#else
using std::cin;
using std::cout;
#endif

int main() {
        int a, b;
        cin >> a >> b;
        cout << a + b << "\n";
        return 0;
}
